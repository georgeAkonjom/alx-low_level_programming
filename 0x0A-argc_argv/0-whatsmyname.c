#include <stdio.h>
/**
 * main - prints the name of its executable.
 *
 * @argc: no of arguments in the argv array.
 *
 * @argv: pointer array. an array of pointers.
 *
 * Return: always o.
 *
 */
int main(int argc, char *argv[])
{
	int i = 0;

	printf("%s\n", argv[i]);
	return (0);
}
